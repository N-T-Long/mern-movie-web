import React from 'react';
import PropTypes from 'prop-types';
import "./style.scss"
import { Container } from 'react-bootstrap';

Profile.propTypes = {
    
};

function Profile(props) {
    return (
        <div >
            <h1 style={{color: "red", margin: "200px"}}>User Page</h1>
        </div>
    );
}

export default Profile;